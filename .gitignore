# secrets — never commit real env files
.env
env/*.env
!env/*.env.example

# mcpo config gets postgres password injected at bootstrap/deploy time — exclude the runtime copy
conf/mcposerver/config.json

# runtime data
data/
logs/
*.log

# macOS
.DS_Store

# editors
.idea/
.vscode/
*.swp
*.swo
