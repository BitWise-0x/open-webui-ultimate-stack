# =============================================================================
# MCPO — MCP to OpenAPI proxy server
# Docs: https://github.com/open-webui/mcpo
# The postgres connection string is passed via conf/mcposerver/config.json args.
# =============================================================================

# Reference only — not consumed by mcpo directly (connection string is in conf/mcposerver/config.json).
# Kept in sync with config.json by deploy-swarm.sh / bootstrap.sh for debugging convenience.
DATABASE_URL=postgresql://postgres:change_me@db:5432/openwebui
