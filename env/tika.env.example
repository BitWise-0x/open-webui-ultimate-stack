# =============================================================================
# Apache Tika — OCR and document content extraction
# Docs: https://tika.apache.org/
# =============================================================================

# Must match the image tag in docker-compose.yml / .env TIKA_TAG
TIKA_TAG=3.2.2.0
